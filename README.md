# Simple Watermelon Demo

This demo holds a client RN app and a simple node.js API to interact with.

Running API
In the server directory

```
docker-compose up
npm start
```

Running RN App
In the client directory

```
npm i
cd ios && pod install
react-native run-ios
```
